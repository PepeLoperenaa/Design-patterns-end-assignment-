package tickets

import interfaces.TicketInterface

/**
 *Ticket Interface, Implements the changeFlight method to proxyTicket class.
 */
open class FlexTicket : TicketInterface {
    override fun changeFlight(ticketNum: Int) {

    }
}